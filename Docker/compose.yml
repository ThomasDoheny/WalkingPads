services:
  walkingpads:
    container_name: walkingpads
    build:
      context: ..
      dockerfile: Docker/Dockerfile
    volumes:
      - ..:/WalkingPads
    stdin_open: true
    tty: true
    environment:
      - DISPLAY=host.docker.internal:0.0
      - LIBGL_ALWAYS_INDIRECT=0
